import Title from "./Title";

const Header = (props) => {
  return (
    <header>
      <div className="Title">
        <h1>
          <Title name="The Job Board" />
        </h1>
      </div>
    </header>
  );
};

export default Header;
